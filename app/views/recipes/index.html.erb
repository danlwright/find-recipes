
<h1 style="text-align: center" >Recipe Finder</h1>
<h2 style="text-align: center" >Searching for - <%= @search_term.capitalize %></h2>
</br>
<% if @recipes.nil? %>
<p>Uh, Oh! We didn't find anything.  Try something else.</p>

<%else%>
<table class="table"; align="center"; style="text-align: center"; width="75%"; border="1">
  <!-- <tr>
  	<th>Image</th>
  	<th>Title</th>
  	<th>Social Rank</th>
  </tr> -->
 
  <% @recipes.each do |recipe| %>

  	<tr class=<%= cycle('even', 'odd') %>>
  		<td width="15%"><%= link_to(image_tag(recipe["image_url"], height: "175"), recipe["source_url"]) %></td>
  		<td width="45%"><%= link_to(sanitize(recipe["title"]), recipe["source_url"]) %></td>
  		<td width="15%"><%= recipe["social_rank"].round %></td>
  	</tr>
  <% end %>

</table>
<% end %>

<p>Powered By Food2Fork.com</p>
